# AUDA Combined Benchmark — Environment Variables
# ─────────────────────────────────────────────────────────────────────────
# 1. Copy this file:   cp example.env .env
# 2. Fill in your key below
# 3. The notebook loads .env automatically on startup — no extra library needed
#
# .env is in .gitignore; example.env is safe to commit.
# ─────────────────────────────────────────────────────────────────────────

# Groq API key for LLM-powered feature recommendations.
# Free tier available at https://console.groq.com
# Leave empty to use rule-based suggestions only (default).
GROQ_API_KEY=
